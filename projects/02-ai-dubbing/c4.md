# C4 — Сервис AI-озвучки

Три уровня модели C4 плюс модель данных. Машиночитаемый исходник —
[`c4/workspace.dsl`](c4/workspace.dsl) в формате Structurizr DSL.

---

## Уровень 1. Контекст системы

```mermaid
flowchart TB
    producer(["👤 Продюсер<br/><i>ставит задачу, принимает результат</i>"])
    editor(["👤 Технический редактор<br/><i>правит дефекты озвучки</i>"])
    assessor(["👤 Асессор<br/><i>оценивает качество в MOS-тесте</i>"])

    subgraph boundary[" "]
        system["<b>DubStudio</b><br/><i>Автоматический дубляж видео<br/>на 12 языков</i>"]
    end

    hosting["<b>Видеохостинг</b><br/><i>исходники и публикация<br/>аудиодорожек</i>"]
    stt["<b>STT-провайдер</b><br/><i>распознавание речи<br/>с тайм-кодами</i>"]
    mt["<b>MT-провайдер</b><br/><i>машинный перевод</i>"]
    tts["<b>TTS-провайдер</b><br/><i>синтез речи, SSML</i>"]

    producer -- "ставит задачу,<br/>принимает дорожку" --> system
    editor -- "правит текст, транскрипции<br/>и тайминги" --> system
    assessor -- "ставит оценки MOS" --> system
    system -- "скачивает исходник,<br/>публикует дорожку" --> hosting
    system -- "отправляет аудио" --> stt
    system -- "отправляет транскрипт" --> mt
    system -- "отправляет размеченный текст" --> tts

    classDef person fill:#2b5d8a,stroke:#143349,color:#fff
    classDef core fill:#1a4d7a,stroke:#0d2840,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    class producer,editor,assessor person
    class system core
    class hosting,stt,mt,tts ext
    style boundary fill:none,stroke:#888,stroke-dasharray:5 5
```

### Границы системы

**В скоупе:** распознавание, перевод, фонетическая коррекция, синтез,
сведение, автоконтроль качества, MOS-приёмка, публикация дорожки.

**Вне скоупа:** липсинк под движение губ, дубляж прямых трансляций,
клонирование голоса конкретного диктора, монтаж видеоряда.

---

## Уровень 2. Контейнеры

```mermaid
flowchart TB
    producer(["👤 Продюсер"])
    editor(["👤 Технический редактор"])
    hosting["<b>Видеохостинг</b>"]
    aiapi["<b>STT / MT / TTS<br/>провайдеры</b>"]

    subgraph sys["DubStudio"]
        webapp["<b>Веб-редактор озвучки</b><br/>SPA<br/><i>сегменты, транскрипции,<br/>прослушивание, правки</i>"]
        api["<b>Backend API</b><br/>Python, FastAPI<br/><i>проекты, выпуски,<br/>сегменты, права</i>"]
        orchestrator["<b>Оркестратор пайплайна</b><br/>Python, очередь задач<br/><i>шаги, состояние выпуска,<br/>повторы и компенсация</i>"]
        adapters["<b>Адаптеры провайдеров</b><br/>Python<br/><i>единый контракт поверх<br/>разных API, ретраи, лимиты</i>"]
        audio["<b>Аудио-процессор</b><br/>Python, FFmpeg<br/><i>демукс, нормализация,<br/>сведение, тайминг</i>"]
        phonetics["<b>Модуль фонетики</b><br/>Python<br/><i>глоссарий, SSML,<br/>правила транскрипции</i>"]
        qc["<b>Контроль качества</b><br/>Python<br/><i>WER, рассинхрон,<br/>клиппинг, MOS</i>"]
        db[("<b>БД</b><br/>PostgreSQL<br/><i>проекты, выпуски,<br/>сегменты, глоссарий</i>")]
        storage[("<b>Объектное хранилище</b><br/>S3-совместимое<br/><i>аудио, дорожки, превью</i>")]
    end

    producer -- "HTTPS" --> webapp
    editor -- "HTTPS" --> webapp
    webapp -- "REST" --> api
    api -- "SQL" --> db
    api -- "ставит задачи" --> orchestrator
    orchestrator -- "вызывает шаги" --> audio
    orchestrator -- "вызывает шаги" --> phonetics
    orchestrator -- "вызывает провайдеров" --> adapters
    orchestrator -- "запускает проверки" --> qc
    adapters -- "HTTPS" --> aiapi
    orchestrator -- "скачивает и публикует" --> hosting
    audio -- "читает и пишет" --> storage
    qc -- "SQL" --> db
    orchestrator -- "SQL" --> db

    classDef person fill:#2b5d8a,stroke:#143349,color:#fff
    classDef container fill:#2f3542,stroke:#171b21,color:#fff
    classDef store fill:#3d5a3d,stroke:#1e2d1e,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    class producer,editor person
    class webapp,api,orchestrator,adapters,audio,phonetics,qc container
    class db,storage store
    class hosting,aiapi ext
    style sys fill:none,stroke:#888,stroke-dasharray:5 5
```

### Почему так

**Адаптеры провайдеров вынесены в отдельный контейнер.** Это прямое следствие
задачи сравнения провайдеров: пока адаптер один, сменить TTS означает
переписать пайплайн. Единый внутренний контракт — «текст с разметкой на входе,
аудио и тайм-коды на выходе» — позволяет вести A/B на реальном контенте и
менять провайдера по языкам: для одного языка лучше один вендор, для другого —
другой, и это нормальное состояние системы, а не временное.

**Оркестратор хранит состояние выпуска между шагами.** Распознавание часового
видео стоит денег и минут. Если пайплайн упал на синтезе, он обязан
продолжиться с синтеза, а не с нуля. Каждый шаг идемпотентен и получает на
вход ссылку на артефакт предыдущего шага, а не пересчитывает его.

**Фонетика — отдельный модуль, а не настройка TTS.** Глоссарий терминов и
правила транскрипции — это доменные знания заказчика, которые живут дольше
любого вендора. Они хранятся в системе и применяются к тексту до передачи
провайдеру; при смене TTS глоссарий переезжает вместе с проектом.

**Аудио лежит в объектном хранилище, метаданные — в PostgreSQL.** Дорожка на
час занимает сотни мегабайт; в реляционной базе ей делать нечего. В базе —
сегменты, тайм-коды, версии текста и ссылки на артефакты.

---

## Уровень 3. Компоненты оркестратора и контроля качества

```mermaid
flowchart TB
    api["<b>Backend API</b>"]
    adapters["<b>Адаптеры провайдеров</b>"]
    storage[("<b>Объектное хранилище</b>")]

    subgraph orch["Оркестратор пайплайна"]
        statemachine["<b>Машина состояний выпуска</b><br/><i>шаги, переходы,<br/>точки возобновления</i>"]
        segmenter["<b>Сегментатор</b><br/><i>разбиение по фразам<br/>и паузам</i>"]
        glossary["<b>Применение глоссария</b><br/><i>термины, имена,<br/>аббревиатуры</i>"]
        ssml["<b>Генератор SSML</b><br/><i>транскрипции, паузы,<br/>ударения, темп</i>"]
        timing["<b>Выравнивание тайминга</b><br/><i>сжатие и растяжение<br/>под исходную длительность</i>"]
        mixer["<b>Сведение дорожки</b><br/><i>громкость, фон,<br/>склейка сегментов</i>"]
    end

    subgraph qcbox["Контроль качества"]
        wer["<b>Расчёт WER</b><br/><i>обратное распознавание<br/>синтезированной дорожки</i>"]
        drift["<b>Детектор рассинхрона</b><br/><i>отклонение тайм-кодов<br/>от исходных</i>"]
        loud["<b>Проверка громкости</b><br/><i>LUFS, клиппинг,<br/>тишина</i>"]
        mos["<b>MOS-тест</b><br/><i>выборка сегментов,<br/>сбор оценок асессоров</i>"]
        verdict["<b>Вердикт приёмки</b><br/><i>сводный порог<br/>по всем метрикам</i>"]
    end

    api --> statemachine
    statemachine --> segmenter
    segmenter --> glossary
    glossary --> ssml
    ssml --> adapters
    adapters --> timing
    timing --> mixer
    mixer --> storage
    statemachine --> wer
    statemachine --> drift
    statemachine --> loud
    wer --> verdict
    drift --> verdict
    loud --> verdict
    mos --> verdict
    verdict --> statemachine

    classDef comp fill:#2f3542,stroke:#171b21,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    classDef store fill:#3d5a3d,stroke:#1e2d1e,color:#fff
    class statemachine,segmenter,glossary,ssml,timing,mixer,wer,drift,loud,mos,verdict comp
    class api,adapters ext
    class storage store
    style orch fill:none,stroke:#888,stroke-dasharray:5 5
    style qcbox fill:none,stroke:#888,stroke-dasharray:5 5
```

### Метрики приёмки

| Метрика | Что измеряет | Как считается | Порог |
|---|---|---|---|
| WER | Насколько синтез произносит то, что написано | Обратное распознавание готовой дорожки и сравнение с исходным текстом | ≤ 5% |
| Рассинхрон | Уход дорожки от картинки | Максимальное отклонение начала сегмента от исходного тайм-кода | ≤ 300 мс |
| Отклонение длительности | Перелив реплики за отведённое окно | Разница длительности сегмента и исходного | ≤ 15% |
| Громкость | Пригодность к публикации | Интегральная громкость по LUFS, наличие клиппинга | −16 ± 1 LUFS, без клиппинга |
| MOS | Субъективная естественность | Средняя оценка 1–5 на выборке ≥ 20 сегментов, ≥ 5 асессоров | ≥ 4.0 |

**Почему WER считается обратным распознаванием, а не сравнением с
транскриптом.** Ошибка может возникнуть на любом шаге: STT неверно распознал,
MT потерял отрицание, TTS проглотил окончание. Прогон готовой дорожки через
распознавание проверяет результат целиком, а не отдельное звено — это
сквозная проверка, а не проверка компонента.

---

## Модель данных

```mermaid
erDiagram
    PROJECT ||--o{ VIDEO : "содержит"
    VIDEO ||--o{ DUB_JOB : "дублируется в"
    DUB_JOB ||--o{ SEGMENT : "состоит из"
    SEGMENT ||--o{ SEGMENT_VERSION : "имеет версии"
    DUB_JOB ||--o{ QC_RESULT : "проверяется"
    DUB_JOB ||--o{ MOS_SCORE : "оценивается"
    PROJECT ||--o{ GLOSSARY_ENTRY : "владеет"
    DUB_JOB }o--|| VOICE_PROFILE : "использует"

    PROJECT {
        uuid id PK
        string name
        string source_language
    }
    VIDEO {
        uuid id PK
        uuid project_id FK
        string hosting_id UK
        int duration_ms
    }
    DUB_JOB {
        uuid id PK
        uuid video_id FK
        string target_language
        string state
        uuid voice_profile_id FK
        timestamptz created_at
    }
    SEGMENT {
        uuid id PK
        uuid dub_job_id FK
        int index
        int start_ms
        int end_ms
        text source_text
    }
    SEGMENT_VERSION {
        uuid id PK
        uuid segment_id FK
        int revision
        text translated_text
        text ssml
        string audio_key
        string author
    }
    QC_RESULT {
        uuid id PK
        uuid dub_job_id FK
        string metric
        numeric value
        boolean passed
    }
    MOS_SCORE {
        uuid id PK
        uuid dub_job_id FK
        uuid segment_id FK
        string assessor
        int score
    }
    GLOSSARY_ENTRY {
        uuid id PK
        uuid project_id FK
        string term
        string language
        string transcription
        string do_not_translate
    }
    VOICE_PROFILE {
        uuid id PK
        string provider
        string voice_id
        string language
        numeric speed
    }
```

**Сегмент и его версии разделены намеренно.** Правка редактора не затирает
машинный вариант: она создаёт новую ревизию. Это даёт три вещи сразу —
возможность откатиться, материал для оценки того, как часто и что именно
приходится править вручную, и данные для настройки глоссария. Через месяц
работы видно, какие термины система стабильно произносит неправильно, и они
уходят в `GLOSSARY_ENTRY` вместо того, чтобы правиться каждый выпуск.

**`VOICE_PROFILE` привязан к выпуску, а не к проекту.** Один и тот же курс на
испанском и на японском может звучать голосами разных вендоров — это прямой
результат сравнения провайдеров по языкам.

---

## Сравнение TTS/STT-провайдеров: критерии выбора

Матрица, по которой принималось решение. Значения заполняются на пилоте
с реальным контентом заказчика, а не берутся из маркетинговых материалов
вендора.

| Критерий | Почему важен | Как проверялся |
|---|---|---|
| Покрытие языков | 12 целевых языков одним вендором дешевле в поддержке | Список поддерживаемых голосов на дату пилота |
| Поддержка SSML | Без разметки нет управления произношением терминов | Тест: транскрипция, пауза, ударение, темп |
| Качество на доменной лексике | Общий бенчмарк ничего не говорит о вашем контенте | MOS на 20 сегментах реального курса |
| Тайм-коды на выходе STT | Без них невозможно выровнять дорожку | Проверка формата ответа |
| Латентность и лимиты | Определяют время дубляжа часового видео | Замер на пакете из 10 роликов |
| Цена за минуту | Прямо влияет на целевые −75% | Расчёт на реальном объёме, а не на прайсе |
| Стабильность API | Отказы вендора становятся отказами продукта | Доля ошибок за две недели пилота |
| Условия по данным | Куда уезжает контент заказчика и хранится ли он | Юридическая проверка условий обработки |

Решение по итогам: единый внутренний контракт адаптера и возможность
назначать разного вендора на разные языки. Ни один провайдер не оказался
лучшим по всем двенадцати языкам одновременно — и архитектура не должна
делать вид, что оказался.

---

## Как открыть DSL

```bash
docker run -it --rm -p 8080:8080 \
  -v "$PWD/projects/02-ai-dubbing/c4:/usr/local/structurizr" \
  structurizr/lite
```
