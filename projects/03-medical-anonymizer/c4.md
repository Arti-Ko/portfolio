# C4 — Анонимизатор медицинских данных

Три уровня модели C4, модель данных и разделение контуров хранения.
Машиночитаемый исходник — [`c4/workspace.dsl`](c4/workspace.dsl).

---

## Уровень 1. Контекст системы

```mermaid
flowchart TB
    doctor(["👤 Врач<br/><i>работает в МИС,<br/>сервиса не видит</i>"])
    dpo(["👤 Оператор ПДн<br/><i>верифицирует спорные случаи,<br/>согласует раскрытие</i>"])
    researcher(["👤 Исследователь<br/><i>получает обезличенные данные</i>"])
    security(["👤 Служба безопасности<br/><i>читает аудит-лог</i>"])

    subgraph boundary[" "]
        system["<b>Анонимизатор</b><br/><i>Распознавание и маскирование ПДн<br/>в медицинских документах</i>"]
    end

    mis["<b>МИС</b><br/><i>медицинская<br/>информационная система</i>"]
    idp["<b>Корпоративный IdP</b><br/><i>аутентификация<br/>сотрудников</i>"]
    siem["<b>SIEM</b><br/><i>сбор событий<br/>безопасности</i>"]

    doctor -- "создаёт документ" --> mis
    mis -- "отправляет документ<br/>на обезличивание" --> system
    system -- "возвращает<br/>обезличенный документ" --> mis
    dpo -- "верифицирует разметку,<br/>согласует раскрытие" --> system
    researcher -- "запрашивает<br/>обезличенные данные" --> system
    system -- "передаёт события" --> siem
    security -- "расследует инциденты" --> siem
    system -- "проверяет права" --> idp

    classDef person fill:#2b5d8a,stroke:#143349,color:#fff
    classDef core fill:#1a4d7a,stroke:#0d2840,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    class doctor,dpo,researcher,security person
    class system core
    class mis,idp,siem ext
    style boundary fill:none,stroke:#888,stroke-dasharray:5 5
```

### Границы системы

**В скоупе:** приём документа из МИС, распознавание и классификация ПДн,
маскирование и псевдонимизация, контрольная проверка результата, карантин,
хранение токен-мапы, аудит, обратная идентификация по обоснованному запросу.

**Вне скоупа:** сбор согласия пациента на обработку (фиксируется в МИС до
обращения к сервису), обучение и дообучение NLP-моделей (отдельный контур с
собственным процессом разметки), формирование исследовательских датасетов с
агрегацией и k-анонимностью (другие правила, другой процесс).

**Ключевое свойство контекста: врач не взаимодействует с системой напрямую.**
Требование «встроиться в существующий документооборот» означает, что рабочий
процесс врача не меняется вообще — обезличивание происходит на стороне МИС по
API. Любой сценарий, где врач должен что-то нажать, нарушает цель проекта.

---

## Уровень 2. Контейнеры

```mermaid
flowchart TB
    mis["<b>МИС</b>"]
    dpo(["👤 Оператор ПДн"])
    siem["<b>SIEM</b>"]

    subgraph sys["Анонимизатор"]
        gw["<b>API-шлюз</b><br/>Python, FastAPI<br/><i>mTLS, проверка токена,<br/>валидация схемы, лимиты</i>"]
        pipeline["<b>NLP-пайплайн</b><br/>Python<br/><i>извлечение текста, NER,<br/>классификация, маскирование</i>"]
        verifier["<b>Контрольный верификатор</b><br/>Python<br/><i>независимый поиск<br/>остаточных ПДн</i>"]
        console["<b>Консоль оператора</b><br/>Web<br/><i>верификация разметки,<br/>карантин, согласования</i>"]
        reid["<b>Сервис деанонимизации</b><br/>Python<br/><i>выборочное раскрытие<br/>токен-мапы</i>"]
        docdb[("<b>Хранилище документов</b><br/>PostgreSQL<br/><i>обезличенные тексты<br/>и разметка</i>")]
        vault[("<b>Хранилище токен-мапы</b><br/>PostgreSQL, отдельный контур<br/><i>соответствие токен → значение</i>")]
        audit[("<b>Аудит-лог</b><br/>append-only<br/><i>неизменяемые записи<br/>операций с ПДн</i>")]
    end

    mis -- "POST /anonymize<br/>mTLS" --> gw
    gw -- "передаёт документ" --> pipeline
    pipeline -- "пишет результат" --> docdb
    pipeline -- "пишет токены" --> vault
    pipeline -- "запускает проверку" --> verifier
    verifier -- "читает результат" --> docdb
    dpo -- "HTTPS" --> console
    console -- "читает и правит разметку" --> docdb
    console -- "инициирует раскрытие" --> reid
    reid -- "читает по разрешённым полям" --> vault
    gw -- "пишет событие" --> audit
    pipeline -- "пишет событие" --> audit
    reid -- "пишет событие" --> audit
    audit -- "экспортирует события" --> siem

    classDef person fill:#2b5d8a,stroke:#143349,color:#fff
    classDef container fill:#2f3542,stroke:#171b21,color:#fff
    classDef store fill:#3d5a3d,stroke:#1e2d1e,color:#fff
    classDef vaultc fill:#6b2d2d,stroke:#3a1616,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    class dpo person
    class gw,pipeline,verifier,console,reid container
    class docdb,audit store
    class vault vaultc
    class mis,siem ext
    style sys fill:none,stroke:#888,stroke-dasharray:5 5
```

### Три контура хранения

Красным на схеме выделено хранилище токен-мапы — единственное место в
системе, где обезличенные данные можно связать с реальным пациентом.
Разделение контуров — не архитектурная эстетика, а способ ограничить
последствия компрометации.

| Контур | Что хранит | Кто имеет доступ | Что даёт компрометация |
|---|---|---|---|
| Документы | Обезличенные тексты, разметку, метаданные | Пайплайн, консоль оператора, исследователи через выгрузку | Тексты без идентификаторов — сами по себе не ПДн |
| Токен-мапа | Соответствие «токен → реальное значение» | Только сервис деанонимизации, только по согласованному запросу | Прямое раскрытие пациентов — поэтому отдельный ключ, отдельная сеть, отдельные права |
| Аудит | Кто, что, когда и на каком основании делал | Служба безопасности на чтение; запись — только append | Знание о действиях, но не сами данные |

**Верификатор намеренно не переиспользует код пайплайна.** Если бы
контрольный прогон использовал ту же модель и те же правила, он повторял бы
её ошибки и всегда возвращал «чисто». Независимость метода — условие
осмысленности проверки: пайплайн опирается на нейросетевой NER, верификатор —
на регулярные выражения, словари и контекстные правила.

---

## Уровень 3. Компоненты NLP-пайплайна

```mermaid
flowchart TB
    gw["<b>API-шлюз</b>"]
    docdb[("<b>Хранилище документов</b>")]
    vault[("<b>Токен-мапа</b>")]

    subgraph pipe["NLP-пайплайн"]
        extract["<b>Извлечение текста</b><br/><i>PDF, DOCX, HL7 v2,<br/>FHIR, простой текст</i>"]
        normalize["<b>Нормализация</b><br/><i>кодировки, переносы,<br/>структура документа</i>"]
        ner["<b>NER-модель</b><br/><i>ФИО, даты, адреса,<br/>организации, контакты</i>"]
        rules["<b>Правила и словари</b><br/><i>СНИЛС, полис ОМС,<br/>номера карт, телефоны</i>"]
        context["<b>Контекстный анализ</b><br/><i>врач или пациент,<br/>дата рождения или приёма</i>"]
        classify["<b>Классификация</b><br/><i>категории ФЗ-152,<br/>18 идентификаторов HIPAA</i>"]
        confidence["<b>Оценка уверенности</b><br/><i>порог передачи<br/>оператору</i>"]
        mask["<b>Маскирование</b><br/><i>стратегия по типу:<br/>замена, сдвиг, обобщение</i>"]
        tokenize["<b>Токенизация</b><br/><i>устойчивые псевдонимы<br/>внутри документа</i>"]
    end

    gw --> extract
    extract --> normalize
    normalize --> ner
    normalize --> rules
    ner --> context
    rules --> context
    context --> classify
    classify --> confidence
    confidence --> mask
    mask --> tokenize
    mask --> docdb
    tokenize --> vault

    classDef comp fill:#2f3542,stroke:#171b21,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    classDef store fill:#3d5a3d,stroke:#1e2d1e,color:#fff
    classDef vaultc fill:#6b2d2d,stroke:#3a1616,color:#fff
    class extract,normalize,ner,rules,context,classify,confidence,mask,tokenize comp
    class gw ext
    class docdb store
    class vault vaultc
    style pipe fill:none,stroke:#888,stroke-dasharray:5 5
```

### Стратегии маскирования по типам идентификаторов

Единая стратегия «заменить всё на `[СКРЫТО]`» уничтожает данные. Разные типы
идентификаторов требуют разного обращения — это ключевое проектное решение,
от которого зависит, останутся ли данные пригодными для исследований.

| Тип | Стратегия | Почему так |
|---|---|---|
| ФИО пациента | Устойчивый псевдоним в пределах документа | «Иванов» в анамнезе и «Иванову» в назначении — один человек; связность текста должна сохраниться |
| ФИО врача | Замена на роль («лечащий врач») | Врач — не субъект исследования, но его специальность может быть значима |
| Дата рождения | Сдвиг всех дат пациента на единый случайный интервал | Возраст и интервалы между событиями сохраняются, конкретная дата — нет |
| Даты приёмов и процедур | Тот же сдвиг, что и дата рождения | Иначе рассыпается хронология лечения |
| Возраст старше 89 лет | Обобщение до «90+» | Прямое требование HIPAA Safe Harbor: в этой группе возраст сам по себе идентифицирует |
| Адрес | Обобщение до региона | Регион нужен для эпидемиологии, дом и квартира — нет |
| СНИЛС, полис, номер карты | Полная замена на токен | Прямые идентификаторы без исследовательской ценности |
| Телефон, e-mail | Полное удаление | Контактные данные не нужны в обезличенном документе ни при каком сценарии |
| Редкий диагноз | Флаг на ручную проверку | Само заболевание может идентифицировать пациента в малой популяции — машина этого не решает |

**Сдвиг дат единый для пациента, а не для документа.** Если каждый документ
сдвигать независимо, разрушается хронология между документами одного
пациента. Величина сдвига хранится в токен-мапе вместе с остальными
соответствиями.

---

## Модель данных

```mermaid
erDiagram
    REQUEST ||--|| DOCUMENT : "порождает"
    DOCUMENT ||--o{ ENTITY : "содержит"
    ENTITY ||--o| TOKEN_MAP : "раскрывается через"
    DOCUMENT ||--o{ VERIFICATION : "проверяется"
    DOCUMENT }o--o| QUARANTINE_CASE : "может попасть в"
    REQUEST ||--o{ AUDIT_EVENT : "фиксируется"
    DISCLOSURE_REQUEST ||--o{ AUDIT_EVENT : "фиксируется"
    DISCLOSURE_REQUEST }o--|| TOKEN_MAP : "раскрывает"

    REQUEST {
        uuid id PK
        string source_system
        string client_cert_fingerprint
        timestamptz received_at
        string status
    }
    DOCUMENT {
        uuid id PK
        uuid request_id FK
        string format
        text anonymized_text
        string patient_pseudonym
        int date_shift_days
        timestamptz processed_at
    }
    ENTITY {
        uuid id PK
        uuid document_id FK
        string entity_type
        int start_offset
        int end_offset
        numeric confidence
        string strategy
        boolean manually_verified
    }
    TOKEN_MAP {
        uuid id PK
        string token UK
        bytea encrypted_value
        string entity_type
        uuid patient_ref
    }
    VERIFICATION {
        uuid id PK
        uuid document_id FK
        string method
        int residual_found
        boolean passed
    }
    QUARANTINE_CASE {
        uuid id PK
        uuid document_id FK
        string reason
        string assigned_to
        string resolution
    }
    DISCLOSURE_REQUEST {
        uuid id PK
        string requester
        text legal_basis
        string requested_fields
        string approver_1
        string approver_2
        timestamptz expires_at
        string status
    }
    AUDIT_EVENT {
        uuid id PK
        string actor
        string action
        string object_ref
        text reason
        timestamptz occurred_at
        string prev_hash
    }
```

**`AUDIT_EVENT.prev_hash` делает лог неизменяемым по построению.** Каждая
запись содержит хеш предыдущей — цепочка. Удалить или отредактировать
запись задним числом нельзя, не сломав все последующие. Это дешевле
специализированного WORM-хранилища и достаточно, чтобы факт вмешательства
был обнаружим.

**`ENTITY` хранит смещения, а не сами значения.** В хранилище документов не
должно быть ни одного фрагмента исходных персональных данных — даже в
служебных полях разметки. Соответствие «токен → значение» живёт только в
зашифрованном виде в отдельном контуре.

**`TOKEN_MAP.patient_ref` позволяет выборочное раскрытие.** Запрос на связь с
лечащим врачом раскрывает одно поле, а не весь профиль пациента. Без этой
связи пришлось бы раскрывать токен-мапу документа целиком.

---

## Как открыть DSL

```bash
docker run -it --rm -p 8080:8080 \
  -v "$PWD/projects/03-medical-anonymizer/c4:/usr/local/structurizr" \
  structurizr/lite
```
