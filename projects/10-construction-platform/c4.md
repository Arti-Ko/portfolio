# C4 — B2B-платформа управления строительными проектами

Машиночитаемый исходник — [`c4/workspace.dsl`](c4/workspace.dsl).

Архитектура описана для целевого продукта. Прототип, на который были
исходные требования, реализует интерфейсный слой на статичных данных —
см. [requirements-log.md](requirements-log.md).

---

## Уровень 1. Контекст

```mermaid
flowchart TB
    client(["👤 Клиент<br/><i>создаёт ТЗ,<br/>принимает этапы</i>"])
    contractor(["👤 Подрядчик<br/><i>подаёт КП,<br/>сдаёт работы</i>"])
    manager(["👤 Менеджер платформы<br/><i>ведёт проект,<br/>управляет выплатами</i>"])

    subgraph boundary[" "]
        system["<b>Платформа управления<br/>стройпроектами</b><br/><i>ТЗ, тендер, этапы,<br/>приёмка, выплаты</i>"]
    end

    escrow["<b>Эскроу-провайдер</b><br/><i>блокировка средств<br/>и выплаты</i>"]
    registries["<b>Реестры</b><br/><i>проверка компаний<br/>при аккредитации</i>"]
    storage["<b>Хранилище файлов</b><br/><i>чертежи, фотоотчёты,<br/>документы</i>"]

    client --> system
    contractor --> system
    manager --> system
    system -- "блокировка и выплата" --> escrow
    system -- "проверка подрядчиков" --> registries
    system -- "документы и отчёты" --> storage

    classDef person fill:#2b5d8a,stroke:#143349,color:#fff
    classDef corec fill:#1a4d7a,stroke:#0d2840,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    class client,contractor,manager person
    class system corec
    class escrow,registries,storage ext
    style boundary fill:none,stroke:#888,stroke-dasharray:5 5
```

### Границы

**В скоупе:** создание и валидация ТЗ, закрытый тендер, сравнение
предложений, таблица этапов, эскроу-цикл этапа, приёмка, чат проекта,
документы, история действий.

**Вне скоупа прототипа:** реальная банковская интеграция, аккредитация
подрядчиков, юридическое подписание договоров, публичный каталог,
рейтинги и отзывы, мобильная версия под телефон.

**Ключевая особенность контекста:** менеджер платформы — не наблюдатель, а
участник каждой сделки. Он валидирует ТЗ, приглашает подрядчиков,
проверяет приёмку и инициирует выплату. Это отличает продукт от
маркетплейса и определяет, что админ-панель — самый насыщенный из трёх
интерфейсов, а не служебный экран.

---

## Уровень 2. Контейнеры

```mermaid
flowchart TB
    client(["👤 Клиент"])
    contractor(["👤 Подрядчик"])
    manager(["👤 Менеджер"])
    escrow["<b>Эскроу-провайдер</b>"]

    subgraph sys["Платформа"]
        clientapp["<b>Кабинет клиента</b><br/>Web<br/><i>ТЗ, тендер, этапы,<br/>приёмка</i>"]
        contractorapp["<b>Кабинет подрядчика</b><br/>Web<br/><i>приглашения, КП,<br/>сдача этапов</i>"]
        adminapp["<b>Админ-панель</b><br/>Web<br/><i>управление проектами<br/>и статусами</i>"]
        api["<b>Backend API</b><br/><i>проекты, этапы, роли,<br/>переходы состояний</i>"]
        workflow["<b>Движок состояний</b><br/><i>допустимые переходы<br/>проекта и этапа</i>"]
        finance["<b>Финансовый модуль</b><br/><i>блокировка, выплата,<br/>сверка</i>"]
        chat["<b>Чат проекта</b><br/><i>переписка сторон<br/>и платформы</i>"]
        audit["<b>История действий</b><br/><i>кто, что и когда<br/>изменил</i>"]
        db[("<b>Основная БД</b><br/><i>проекты, этапы, КП,<br/>участники</i>")]
        files[("<b>Хранилище документов</b><br/><i>чертежи, фотоотчёты,<br/>акты</i>")]
    end

    client --> clientapp
    contractor --> contractorapp
    manager --> adminapp
    clientapp -- "REST" --> api
    contractorapp -- "REST" --> api
    adminapp -- "REST" --> api
    api -- "проверка перехода" --> workflow
    api -- "SQL" --> db
    api --> chat
    api --> files
    api -- "все изменения" --> audit
    api --> finance
    finance -- "блокировка и выплата" --> escrow

    classDef person fill:#2b5d8a,stroke:#143349,color:#fff
    classDef container fill:#2f3542,stroke:#171b21,color:#fff
    classDef store fill:#3d5a3d,stroke:#1e2d1e,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    class client,contractor,manager person
    class clientapp,contractorapp,adminapp,api,workflow,finance,chat,audit container
    class db,files store
    class escrow ext
    style sys fill:none,stroke:#888,stroke-dasharray:5 5
```

### Почему так

**Три отдельных приложения, а не одно с переключением ролей.** У клиента,
подрядчика и менеджера принципиально разная функциональность: клиент
создаёт ТЗ и принимает работы, подрядчик подаёт предложения и сдаёт
этапы, менеджер управляет всем. Общий интерфейс с условным отображением
превратился бы в набор скрытых блоков, где половина кода отвечает на
вопрос «а можно ли это показать».

В прототипе для демонстрации добавлен переключатель ролей — это
осознанное отступление от продуктовой логики ради показа
([решение 6](requirements-log.md#решение-6-вход-и-переключение-ролей-сосуществуют)).

**Движок состояний вынесен отдельно.** Проект и этап имеют строго
определённые статусы и переходы между ними. Если проверять допустимость
перехода в коде каждого обработчика, правила разъедутся: где-то забудут
запретить оплату уже оплаченного этапа. Единый движок отвечает на вопрос
«можно ли перейти из А в Б» в одном месте.

**Финансовый модуль изолирован от остальной логики.** Через него проходят
операции с деньгами: блокировка, выплата, сверка. Изоляция даёт
возможность отдельно логировать, отдельно тестировать и отдельно
проверять эти операции. В прототипе модуль подменяется макетами, но
граница проведена сразу
([решение 1](requirements-log.md#решение-1-эскроу-реальный-или-макет)).

**История действий пишется на уровне API, а не в каждом модуле.** Любое
изменение статуса, суммы или срока попадает в журнал автоматически. При
споре о том, кто изменил сумму этапа, ответ должен существовать.

---

## Уровень 3. Компоненты движка состояний

```mermaid
flowchart TB
    api["<b>Backend API</b>"]
    finance["<b>Финансовый модуль</b>"]
    audit["<b>История действий</b>"]

    subgraph wf["Движок состояний"]
        projstate["<b>Состояния проекта</b><br/><i>черновик, тендер,<br/>в работе, завершён</i>"]
        stagestate["<b>Состояния этапа</b><br/><i>шесть статусов<br/>и переходы</i>"]
        guards["<b>Условия перехода</b><br/><i>кто и при каких условиях<br/>может перевести</i>"]
        effects["<b>Действия при переходе</b><br/><i>что происходит:<br/>блокировка, уведомление</i>"]
        rollback["<b>Возврат на доработку</b><br/><i>новый цикл этапа<br/>с сохранением истории</i>"]
    end

    api --> projstate
    api --> stagestate
    projstate --> guards
    stagestate --> guards
    guards --> effects
    effects --> finance
    effects --> audit
    stagestate --> rollback
    rollback --> stagestate

    classDef comp fill:#2f3542,stroke:#171b21,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    class projstate,stagestate,guards,effects,rollback comp
    class api,finance,audit ext
    style wf fill:none,stroke:#888,stroke-dasharray:5 5
```

**Условия перехода отделены от действий при переходе.** «Кто имеет право
перевести этап в этот статус» и «что произойдёт при переводе» — разные
вопросы. Клиент может подтвердить приёмку, но выплату инициирует
менеджер: право на переход и следствие перехода принадлежат разным ролям.

**Возврат на доработку моделируется как новый цикл, а не откат.** Этап
получает новую итерацию, предыдущая сохраняется. Иначе теряется главный
показатель качества работы подрядчика — сколько раз этап возвращали.

---

## Модель состояний

### Статусы проекта

| Статус | Когда наступает |
|---|---|
| Черновик | ТЗ создано, ожидает валидации менеджером |
| Тендер | Тендер открыт, идёт сбор и выбор предложений |
| В работе | Подрядчик выбран, идёт выполнение этапов |
| На приёмке | Этап сдан и проходит проверку |
| Завершён | Все этапы приняты и оплачены |

Архив — не статус, а признак завершённого проекта
([решение 4](requirements-log.md#решение-4-архивирование--признак-а-не-статус)).

### Статусы этапа

```mermaid
stateDiagram-v2
    [*] --> ТребуетОплаты
    ТребуетОплаты --> Оплачен : клиент оплатил, средства в эскроу
    Оплачен --> ВРаботе : подрядчик начал работу
    ВРаботе --> НаПроверке : запрошена приёмка с отчётом
    НаПроверке --> НаДоработке : менеджер вернул
    НаДоработке --> ВРаботе : новый цикл
    НаПроверке --> ПринятИОплачен : клиент подтвердил, менеджер выплатил
    ПринятИОплачен --> [*]
```

Единый перечень из шести статусов заменил два конфликтующих набора из
исходных требований
([решение 2](requirements-log.md#решение-2-единый-перечень-статусов-этапа)).

---

## Модель данных

```mermaid
erDiagram
    CLIENT ||--o{ PROJECT : "создаёт"
    PROJECT ||--|| BRIEF : "описан в"
    PROJECT ||--o{ TENDER_INVITE : "рассылает"
    TENDER_INVITE }o--|| CONTRACTOR : "адресовано"
    PROJECT ||--o{ PROPOSAL : "собирает"
    PROPOSAL ||--o{ PROPOSAL_STAGE : "разбито на"
    PROJECT ||--o{ STAGE : "выполняется по"
    STAGE ||--o{ STAGE_CYCLE : "проходит циклы"
    STAGE_CYCLE ||--o{ REPORT : "сдаётся с"
    STAGE ||--o| ESCROW_HOLD : "обеспечен"
    PROJECT ||--|| CHAT : "имеет"
    PROJECT ||--o{ ACTION_LOG : "фиксирует"

    CLIENT {
        uuid id PK
        string company_name
        string contact_ref
    }
    CONTRACTOR {
        uuid id PK
        string company_name
        string accreditation_status
        date accredited_until
    }
    PROJECT {
        uuid id PK
        uuid client_id FK
        uuid contractor_id FK
        string status
        string priority
        boolean archived
    }
    BRIEF {
        uuid id PK
        uuid project_id FK
        text description
        numeric budget_from
        numeric budget_to
        date desired_start
        date desired_end
    }
    TENDER_INVITE {
        uuid id PK
        uuid project_id FK
        uuid contractor_id FK
        timestamptz invited_at
        date response_due
    }
    PROPOSAL {
        uuid id PK
        uuid project_id FK
        uuid contractor_id FK
        numeric total_amount
        int total_days
        string status
    }
    PROPOSAL_STAGE {
        uuid id PK
        uuid proposal_id FK
        int order_index
        string title
        numeric amount
        int days
    }
    STAGE {
        uuid id PK
        uuid project_id FK
        int order_index
        string title
        numeric amount
        string status
    }
    STAGE_CYCLE {
        uuid id PK
        uuid stage_id FK
        int iteration
        timestamptz started_at
        timestamptz submitted_at
        string outcome
    }
    REPORT {
        uuid id PK
        uuid cycle_id FK
        string file_key
        string media_type
    }
    ESCROW_HOLD {
        uuid id PK
        uuid stage_id FK
        numeric amount
        string state
        timestamptz held_at
        timestamptz released_at
    }
    ACTION_LOG {
        uuid id PK
        uuid project_id FK
        string actor_ref
        string action
        text details
        timestamptz occurred_at
    }
```

**`PROPOSAL_STAGE` и `STAGE` — разные сущности.** Первое — этапы в
предложении подрядчика, которые могут не выиграть. Второе — рабочие этапы
проекта, сформированные из выбранного предложения. Хранить их одной
таблицей означало бы, что этапы проигравших предложений живут в проекте.

**`STAGE_CYCLE` отделён от `STAGE`.** Этап, вернувшийся на доработку, —
это новая итерация с новым отчётом. Хранить даты и отчёт полями этапа —
значит затирать историю при каждом возврате и потерять показатель, сколько
раз работу переделывали.

**`ESCROW_HOLD.state` фиксирует состояние денег отдельно от статуса
этапа.** Статус этапа — про работу, состояние блокировки — про деньги. Их
рассинхронизация возможна при сбое внешнего провайдера, и система должна
уметь это увидеть, а не считать, что раз этап оплачен, то средства
заблокированы.

**`CONTRACTOR.accredited_until`.** Аккредитация имеет срок; подрядчик с
истёкшей не должен получать приглашения в новые тендеры, даже если ранее
работал успешно.

---

## Как открыть DSL

```bash
docker run -it --rm -p 8080:8080 \
  -v "$PWD/projects/10-construction-platform/c4:/usr/local/structurizr" \
  structurizr/lite
```
