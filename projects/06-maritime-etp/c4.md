# C4 — ЭТП морских грузоперевозок

Машиночитаемый исходник — [`c4/workspace.dsl`](c4/workspace.dsl).

---

## Уровень 1. Контекст

```mermaid
flowchart TB
    cargo(["👤 Грузовладелец<br/><i>ищет судно под груз</i>"])
    ship(["👤 Судовладелец<br/><i>предлагает суда</i>"])
    port(["👤 Администрация порта<br/><i>управляет слотами</i>"])
    terminal(["👤 Владелец терминала<br/><i>погрузка и разгрузка</i>"])

    subgraph boundary[" "]
        system["<b>ЭТП морских перевозок</b><br/><i>Торги, документооборот,<br/>отслеживание рейса</i>"]
    end

    ais["<b>Поставщик данных AIS</b><br/><i>координаты судов<br/>в реальном времени</i>"]
    crypto["<b>Удостоверяющий центр</b><br/><i>квалифицированная<br/>электронная подпись</i>"]
    registries["<b>Реестры</b><br/><i>компании, суда,<br/>ледовые классы, допуски</i>"]
    charts["<b>Картографический сервис</b><br/><i>морские карты<br/>и ледовая обстановка</i>"]

    cargo -- "публикует лоты,<br/>следит за грузом" --> system
    ship -- "участвует в торгах,<br/>ведёт рейсы" --> system
    port -- "подтверждает слоты" --> system
    terminal -- "фиксирует погрузку<br/>и разгрузку" --> system
    system -- "получает координаты" --> ais
    system -- "подписывает документы" --> crypto
    system -- "проверяет участников" --> registries
    system -- "отображает обстановку" --> charts

    classDef person fill:#2b5d8a,stroke:#143349,color:#fff
    classDef corec fill:#1a4d7a,stroke:#0d2840,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    class cargo,ship,port,terminal person
    class system corec
    class ais,crypto,registries,charts ext
    style boundary fill:none,stroke:#888,stroke-dasharray:5 5
```

### Границы

**В скоупе:** публикация лотов, двухэтапные торги, проверка участников,
формирование и подписание документов, отслеживание рейса, закрытие сделки,
рейтинги участников.

**Вне скоупа:** платежи и банковские гарантии, претензионная работа,
таможенное оформление, управление слотами внутри порта (площадка
отображает подтверждённый слот, но не распределяет их).

---

## Уровень 2. Контейнеры

```mermaid
flowchart TB
    cargo(["👤 Грузовладелец"])
    ship(["👤 Судовладелец"])
    ais["<b>Поставщик AIS</b>"]
    crypto["<b>Удостоверяющий центр</b>"]
    registries["<b>Реестры</b>"]

    subgraph sys["ЭТП морских перевозок"]
        web["<b>Веб-портал</b><br/>SPA<br/><i>лоты, торги, документы,<br/>карта</i>"]
        mobile["<b>Мобильное приложение</b><br/><i>оперативные действия:<br/>слот, погрузка, статус</i>"]
        api["<b>Backend API</b><br/><i>лоты, сделки, участники,<br/>права доступа</i>"]
        auction["<b>Тендерный движок</b><br/><i>этапы, ставки, таймеры,<br/>определение победителя</i>"]
        verify["<b>Служба проверки участников</b><br/><i>реестры, допуски,<br/>ледовый класс</i>"]
        docs["<b>Документооборот</b><br/><i>шаблоны, версии,<br/>маршруты подписания</i>"]
        tracking["<b>Служба отслеживания</b><br/><i>приём AIS, история трека,<br/>детекция отклонений</i>"]
        gis["<b>ГИС-слой</b><br/><i>морские карты,<br/>рендер позиций</i>"]
        db[("<b>Основная БД</b><br/>PostgreSQL<br/><i>лоты, сделки, участники,<br/>документы</i>")]
        tsdb[("<b>Хранилище треков</b><br/><i>координаты судов<br/>во времени</i>")]
    end

    cargo --> web
    ship --> web
    ship --> mobile
    web -- "REST" --> api
    mobile -- "REST" --> api
    api -- "SQL" --> db
    api --> auction
    api --> verify
    api --> docs
    verify -- "запросы" --> registries
    docs -- "подписание" --> crypto
    tracking -- "приём потока" --> ais
    tracking -- "пишет трек" --> tsdb
    tracking -- "события отклонений" --> api
    gis -- "читает позиции" --> tsdb
    web -- "карта" --> gis

    classDef person fill:#2b5d8a,stroke:#143349,color:#fff
    classDef container fill:#2f3542,stroke:#171b21,color:#fff
    classDef store fill:#3d5a3d,stroke:#1e2d1e,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    class cargo,ship person
    class web,mobile,api,auction,verify,docs,tracking,gis container
    class db,tsdb store
    class ais,crypto,registries ext
    style sys fill:none,stroke:#888,stroke-dasharray:5 5
```

### Почему так

**Тендерный движок вынесен из основного API.** Торги — это работа с
таймерами и конкурентными ставками: несколько участников жмут «подать
предложение» в последние секунды, и порядок обработки определяет
победителя. Такая логика требует строгой сериализации операций и своих
гарантий, отличных от остального CRUD. Смешивать её с обычными запросами —
значит либо тормозить всё, либо получить спорный результат торгов.

**Треки судов лежат отдельно от реляционной базы.** Координаты приходят
непрерывно по всем судам в рейсе: это поток временных рядов с большим
объёмом записи и запросами вида «покажи путь за последние сутки».
Профиль нагрузки принципиально другой, чем у сделок и документов.

**Документооборот — отдельный контейнер с маршрутами подписания.**
Коносамент подписывают несколько сторон в определённом порядке; документ
имеет версии, а подписанная версия неизменяема. Это самостоятельная
предметная область со своей моделью состояний.

**Мобильное приложение решает узкий набор задач.** Портовая администрация
и капитан не работают с торгами со смартфона — им нужно подтвердить слот,
зафиксировать начало погрузки, посмотреть статус. Попытка перенести
веб-функциональность целиком в мобильное приложение удвоила бы стоимость
без пользы.

---

## Уровень 3. Компоненты тендерного движка

```mermaid
flowchart TB
    api["<b>Backend API</b>"]
    verify["<b>Служба проверки</b>"]
    db[("<b>Основная БД</b>")]

    subgraph auc["Тендерный движок"]
        lot["<b>Жизненный цикл лота</b><br/><i>черновик, предотбор,<br/>торги, завершён</i>"]
        prequal["<b>Предотбор</b><br/><i>приём заявок,<br/>критерии допуска</i>"]
        bidding["<b>Приём ставок</b><br/><i>сериализация,<br/>валидация шага</i>"]
        timer["<b>Таймеры этапов</b><br/><i>сроки, продление<br/>при ставке в конце</i>"]
        winner["<b>Определение победителя</b><br/><i>критерии, разрешение<br/>равенства</i>"]
        protocol["<b>Протокол процедуры</b><br/><i>неизменяемая запись<br/>хода торгов</i>"]
    end

    api --> lot
    lot --> prequal
    prequal --> verify
    lot --> bidding
    bidding --> timer
    timer --> winner
    winner --> protocol
    protocol --> db

    classDef comp fill:#2f3542,stroke:#171b21,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    classDef store fill:#3d5a3d,stroke:#1e2d1e,color:#fff
    class lot,prequal,bidding,timer,winner,protocol comp
    class api,verify ext
    class db store
    style auc fill:none,stroke:#888,stroke-dasharray:5 5
```

**Продление таймера при ставке в последние минуты — не украшение.** Без
него торги превращаются в состязание в скорости клика на последней
секунде, а результат зависит от задержки сети участника. Правило «ставка в
последние N минут продлевает этап» — отраслевой стандарт, снимающий этот
класс споров.

**Протокол процедуры неизменяем.** Ход торгов — юридически значимая
запись: кто, когда и какую ставку подал. Оспаривание результата начинается
с запроса протокола, и он должен быть защищён от правки задним числом.

---

## Модель данных

```mermaid
erDiagram
    COMPANY ||--o{ USER_ACCOUNT : "имеет"
    COMPANY ||--o{ VESSEL : "владеет"
    COMPANY ||--o{ LOT : "публикует"
    LOT ||--o{ PREQUAL_APPLICATION : "собирает"
    PREQUAL_APPLICATION }o--|| VESSEL : "предлагает"
    LOT ||--o{ BID : "принимает"
    LOT ||--o| DEAL : "порождает"
    DEAL ||--o{ DOCUMENT : "сопровождается"
    DOCUMENT ||--o{ SIGNATURE : "подписывается"
    DEAL ||--o{ VOYAGE_EVENT : "фиксирует"
    VESSEL ||--o{ TRACK_POINT : "оставляет"
    COMPANY ||--o{ RATING_ENTRY : "накапливает"

    COMPANY {
        uuid id PK
        string legal_id UK
        string role
        string verification_status
        date verified_until
    }
    VESSEL {
        uuid id PK
        uuid company_id FK
        string imo UK
        string ice_class
        int deadweight
    }
    LOT {
        uuid id PK
        uuid company_id FK
        string cargo_type
        string route_from
        string route_to
        date window_start
        date window_end
        string ice_class_required
        string stage
    }
    PREQUAL_APPLICATION {
        uuid id PK
        uuid lot_id FK
        uuid vessel_id FK
        string status
        text rejection_reason
    }
    BID {
        uuid id PK
        uuid lot_id FK
        uuid company_id FK
        numeric amount
        timestamptz placed_at
    }
    DEAL {
        uuid id PK
        uuid lot_id FK
        uuid winner_company_id FK
        numeric final_amount
        string state
    }
    DOCUMENT {
        uuid id PK
        uuid deal_id FK
        string doc_type
        int revision
        string content_hash
        boolean immutable
    }
    SIGNATURE {
        uuid id PK
        uuid document_id FK
        string signer_ref
        timestamptz signed_at
        string certificate_ref
    }
    VOYAGE_EVENT {
        uuid id PK
        uuid deal_id FK
        string event_type
        timestamptz occurred_at
        text details
    }
    TRACK_POINT {
        uuid id PK
        uuid vessel_id FK
        numeric lat
        numeric lon
        timestamptz recorded_at
    }
    RATING_ENTRY {
        uuid id PK
        uuid company_id FK
        uuid deal_id FK
        string criterion
        int score
    }
```

**`DOCUMENT.content_hash` и `immutable` — основа юридической значимости.**
Подписанная версия документа фиксируется хешом и помечается неизменяемой.
Правка создаёт новую ревизию, требующую нового подписания. Без этого
электронный коносамент не имеет силы: подпись должна относиться к
конкретному содержанию.

**`COMPANY.verified_until` — срок годности проверки.** Допуски и
страховое покрытие имеют срок действия. Компания, прошедшая предотбор
полгода назад, могла с тех пор потерять допуск. Дата принудительно
устаревает проверку и требует повторной перед новым лотом.

**`VOYAGE_EVENT` отделён от `TRACK_POINT`.** Координата — сырые данные,
событие — интерпретация: «отклонение от маршрута», «задержка более 12
часов», «смена порта назначения». Событий на порядки меньше, они попадают
в уведомления и в рейтинг, а треки нужны только для карты и разбора.

---

## Матрица ролей и прав

| Действие | Грузовладелец | Судовладелец | Администрация порта | Терминал |
|---|---|---|---|---|
| Создать лот | ✅ | — | — | — |
| Видеть чужие лоты | только свои | все открытые | по своему порту | по своему терминалу |
| Подать заявку на предотбор | — | ✅ | — | — |
| Видеть состав участников предотбора | ✅ после закрытия этапа | только свою заявку | — | — |
| Подать ставку | — | ✅ если допущен | — | — |
| Видеть ставки конкурентов | ✅ | только лучшую цену | — | — |
| Подтвердить слот | — | — | ✅ | — |
| Зафиксировать погрузку | — | ✅ | — | ✅ |
| Подписать коносамент | ✅ | ✅ | — | — |
| Видеть трек судна | ✅ по своей сделке | ✅ по своим судам | ✅ по подходу к порту | — |
| Закрыть сделку | ✅ | ✅ | — | — |

**Судовладелец не видит ставки конкурентов по сумме — только лучшую
цену.** Это осознанное ограничение: раскрытие полной картины позволяет
вычислять стратегию конкурента по серии торгов. Грузовладелец видит всё —
он организатор процедуры и несёт ответственность за её результат.

---

## Как открыть DSL

```bash
docker run -it --rm -p 8080:8080 \
  -v "$PWD/projects/06-maritime-etp/c4:/usr/local/structurizr" \
  structurizr/lite
```
