# C4 — Telegram-бот регистрации на мероприятия

Архитектура описана по модели C4 на трёх уровнях: контекст, контейнеры,
компоненты. Машиночитаемый исходник — [`c4/workspace.dsl`](c4/workspace.dsl)
в формате Structurizr DSL.

Легенда цветов на схемах: синий — люди, тёмно-серый — контейнеры внутри
системы, серый пунктир — внешние системы.

---

## Уровень 1. Контекст системы

Кто пользуется системой и с чем она разговаривает наружу.

```mermaid
flowchart TB
    participant_user(["👤 Участник<br/><i>приходит на мероприятия</i>"])
    organizer(["👤 Организатор<br/><i>заводит события, смотрит отчёты</i>"])

    subgraph boundary[" "]
        system["<b>EventBot</b><br/><i>Регистрация, напоминания<br/>и обратная связь<br/>внутри Telegram</i>"]
    end

    tg["<b>Telegram Bot API</b><br/><i>доставка сообщений,<br/>inline-кнопки, deep links</i>"]
    crm["<b>Google Sheets / Airtable</b><br/><i>CRM-учёт участников</i>"]
    cal["<b>Календарь участника</b><br/><i>iCal-файл встречи</i>"]

    participant_user -- "регистрируется,<br/>отвечает на опрос" --> system
    organizer -- "заводит мероприятие,<br/>выгружает отчёт NPS" --> system
    system -- "отправляет сообщения<br/>и билеты" --> tg
    tg -- "передаёт апдейты<br/>и нажатия кнопок" --> system
    system -- "синхронизирует<br/>участников" --> crm
    system -- "отдаёт .ics" --> cal

    classDef person fill:#2b5d8a,stroke:#143349,color:#fff
    classDef core fill:#1a4d7a,stroke:#0d2840,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    class participant_user,organizer person
    class system core
    class tg,crm,cal ext
    style boundary fill:none,stroke:#888,stroke-dasharray:5 5
```

### Границы системы

В скоуп **входит**: приём заявок, управление вместимостью и листом ожидания,
напоминания, электронный билет, NPS-опрос и сводная отчётность.

В скоуп **не входит**: продажа билетов и эквайринг, регистрация на входе по
QR-коду (сканер — отдельный контур), рассылки по e-mail. Эти границы
зафиксированы намеренно: платёжный контур тянет за собой 54-ФЗ и кассовую
дисциплину, что кратно увеличивает срок и стоимость MVP.

---

## Уровень 2. Контейнеры

Как система устроена внутри — единицы развёртывания и хранения.

```mermaid
flowchart TB
    participant_user(["👤 Участник"])
    organizer(["👤 Организатор"])
    tg["<b>Telegram Bot API</b>"]
    crm["<b>Google Sheets / Airtable</b>"]

    subgraph sys["EventBot"]
        gateway["<b>Bot Gateway</b><br/>Python, aiogram<br/><i>приём webhook-апдейтов,<br/>роутинг команд и callback'ов</i>"]
        fsm[("<b>FSM Store</b><br/>Redis<br/><i>состояние диалога,<br/>черновики анкет</i>")]
        api["<b>Registration API</b><br/>Python, FastAPI<br/><i>бизнес-логика заявок,<br/>вместимости, билетов</i>"]
        db[("<b>Основная БД</b><br/>PostgreSQL<br/><i>мероприятия, участники,<br/>заявки, ответы NPS</i>")]
        sched["<b>Планировщик</b><br/>Celery Beat<br/><i>ежечасный тик,<br/>окна напоминаний</i>"]
        worker["<b>Пул воркеров</b><br/>Celery<br/><i>рассылки, синхронизация,<br/>повторы при сбоях</i>"]
        analytics["<b>Аналитический модуль</b><br/>Python, SQL<br/><i>расчёт NPS,<br/>воронка регистрации</i>"]
        admin["<b>Панель организатора</b><br/>Web<br/><i>мероприятия, списки,<br/>отчёты</i>"]
    end

    participant_user --> tg
    tg -- "webhook<br/>HTTPS" --> gateway
    gateway -- "читает и пишет<br/>состояние" --> fsm
    gateway -- "REST<br/>внутренний" --> api
    api -- "SQL" --> db
    sched -- "ставит задачи<br/>в очередь" --> worker
    worker -- "REST" --> api
    worker -- "sendMessage<br/>HTTPS" --> tg
    worker -- "batchUpdate<br/>HTTPS" --> crm
    analytics -- "SQL, только чтение" --> db
    organizer -- "HTTPS" --> admin
    admin -- "REST" --> api
    admin -- "запрашивает отчёты" --> analytics

    classDef person fill:#2b5d8a,stroke:#143349,color:#fff
    classDef container fill:#2f3542,stroke:#171b21,color:#fff
    classDef store fill:#3d5a3d,stroke:#1e2d1e,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    class participant_user,organizer person
    class gateway,api,sched,worker,analytics,admin container
    class fsm,db store
    class tg,crm ext
    style sys fill:none,stroke:#888,stroke-dasharray:5 5
```

### Почему так

**Gateway отделён от бизнес-логики.** Telegram требует ответа на webhook за
секунды, иначе апдейт переотправляется и пользователь получает дубли. Gateway
делает минимум — разбирает апдейт, читает состояние FSM, вызывает API — и
никогда не ходит во внешние сети синхронно.

**Состояние диалога живёт в Redis, а не в PostgreSQL.** Черновик анкеты
меняется на каждом нажатии; писать это в основную БД — лишняя нагрузка на
хранилище, у которого другой профиль записи и другие требования к сохранности.
Потеря черновика некритична, потеря заявки — критична, и это разные хранилища.

**Рассылки идут через очередь, а не из обработчика.** Telegram ограничивает
скорость отправки: около 30 сообщений в секунду суммарно и 20 в минуту в одну
группу. Воркер с ретраями и бэкоффом — единственный способ разослать
напоминание тысяче участников, не поймав `429 Too Many Requests`.

**Аналитика читает из основной БД в режиме только чтения.** На объёмах проекта
отдельное хранилище не оправдано, но разделение прав доступа не даёт
отчётному запросу заблокировать регистрацию.

---

## Уровень 3. Компоненты Registration API

Внутреннее устройство контейнера, где сосредоточена бизнес-логика.

```mermaid
flowchart TB
    gateway["<b>Bot Gateway</b>"]
    worker["<b>Пул воркеров</b>"]
    db[("<b>PostgreSQL</b>")]

    subgraph api["Registration API"]
        catalog["<b>Каталог мероприятий</b><br/><i>карточка события,<br/>разбор deep link</i>"]
        capacity["<b>Управление вместимостью</b><br/><i>свободные места, дедлайн,<br/>лист ожидания</i>"]
        profile["<b>Профиль участника</b><br/><i>переиспользование данных<br/>между событиями</i>"]
        validation["<b>Валидация</b><br/><i>формат полей, дубли,<br/>нормализация телефона</i>"]
        registration["<b>Заявки</b><br/><i>создание, отмена,<br/>смена статуса</i>"]
        ticket["<b>Билеты</b><br/><i>генерация QR<br/>и .ics-файла</i>"]
        notify["<b>Диспетчер уведомлений</b><br/><i>шаблоны сообщений,<br/>окна напоминаний</i>"]
        survey["<b>NPS-опрос</b><br/><i>шкала, ветка детрактора,<br/>агрегация</i>"]
        sync["<b>Синхронизация с CRM</b><br/><i>маппинг полей,<br/>идемпотентность</i>"]
    end

    gateway --> catalog
    gateway --> registration
    gateway --> survey
    worker --> notify
    worker --> sync
    catalog --> capacity
    registration --> capacity
    registration --> profile
    registration --> validation
    registration --> ticket
    registration --> notify
    capacity --> db
    registration --> db
    profile --> db
    survey --> db
    sync --> db

    classDef comp fill:#2f3542,stroke:#171b21,color:#fff
    classDef ext fill:#4a4f5a,stroke:#2a2e35,color:#fff,stroke-dasharray:4 3
    classDef store fill:#3d5a3d,stroke:#1e2d1e,color:#fff
    class catalog,capacity,profile,validation,registration,ticket,notify,survey,sync comp
    class gateway,worker ext
    class db store
    style api fill:none,stroke:#888,stroke-dasharray:5 5
```

### Ключевые контракты между компонентами

| Компонент | Отвечает за | Не отвечает за |
|---|---|---|
| Управление вместимостью | Единственный источник правды о свободных местах; бронь и её снятие атомарны | Кому отдать освободившееся место — это решает диспетчер уведомлений |
| Заявки | Жизненный цикл статусов: черновик → подтверждена → отменена → посетил / не пришёл | Формат сообщений участнику |
| Диспетчер уведомлений | Какой шаблон, кому и когда отправить | Доставку — это воркер и Telegram API |
| Синхронизация с CRM | Идемпотентную выгрузку: повторный запуск не плодит дубли строк | Формат таблицы организатора — он настраивается маппингом |

**Разделение вместимости и уведомлений — не формальность.** Когда участник
отказывается за 24 часа, освободившееся место нужно отдать первому в листе
ожидания. Если бы эту логику держал компонент вместимости, он должен был бы
знать про Telegram, шаблоны и часовые пояса. Вместо этого он публикует факт
«место освободилось», а решение о приглашении принимает диспетчер.

---

## Модель данных (ключевые сущности)

```mermaid
erDiagram
    EVENT ||--o{ REGISTRATION : "принимает"
    PARTICIPANT ||--o{ REGISTRATION : "подаёт"
    REGISTRATION ||--o| TICKET : "порождает"
    REGISTRATION ||--o{ REMINDER : "триггерит"
    EVENT ||--o{ SURVEY_RESPONSE : "оценивается"
    PARTICIPANT ||--o{ SURVEY_RESPONSE : "оставляет"
    EVENT ||--o{ WAITLIST_ENTRY : "накапливает"

    EVENT {
        uuid id PK
        string title
        timestamptz starts_at
        string timezone
        int capacity
        timestamptz registration_deadline
        string deep_link_code UK
    }
    PARTICIPANT {
        uuid id PK
        bigint telegram_id UK
        string full_name
        string email
        string phone_e164
    }
    REGISTRATION {
        uuid id PK
        uuid event_id FK
        uuid participant_id FK
        string status
        timestamptz created_at
        timestamptz confirmed_at
    }
    TICKET {
        uuid id PK
        uuid registration_id FK
        string qr_payload UK
        timestamptz issued_at
    }
    REMINDER {
        uuid id PK
        uuid registration_id FK
        string window
        timestamptz sent_at
        string response
    }
    SURVEY_RESPONSE {
        uuid id PK
        uuid event_id FK
        uuid participant_id FK
        int score
        text comment
    }
    WAITLIST_ENTRY {
        uuid id PK
        uuid event_id FK
        uuid participant_id FK
        int position
    }
```

Ограничение целостности, которое держит всю модель: уникальный индекс на
паре `(event_id, participant_id)` в `REGISTRATION` со статусом не «отменена».
Повторное нажатие кнопки, дубль webhook-апдейта и параллельный запрос с двух
устройств упираются в базу, а не в проверку в коде.

---

## Как открыть DSL

```bash
# Structurizr Lite в Docker — рендерит workspace.dsl в интерактивные схемы
docker run -it --rm -p 8080:8080 \
  -v "$PWD/projects/01-telegram-event-bot/c4:/usr/local/structurizr" \
  structurizr/lite
```
